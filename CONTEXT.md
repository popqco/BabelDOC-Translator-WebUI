# BabelDOC 桌面科技文档翻译器 领域上下文 (Context & Glossary)

## 术语表 (Glossary)
- **输入文件 (Input File)**：用户添加进待翻译列表的原始 PDF 文档。
- **待翻译队列 (Pending Queue)**：用户通过拖拽或选择加入但尚未开始翻译的文件列表。
- **活动批次 (Active Batch)**：单次批量任务执行上下文，拥有唯一批次 ID（时间戳）与专属独立输出子目录。
- **文档任务 (Document Task)**：批次内对单个 PDF 的处理单元，记录状态（等待中、解析中、翻译中、已完成、失败、已取消）、耗时及错误原因。
- **成果产物 (Artifacts)**：
  - **mono (仅译文版)**：保留原文档版面，文本完全替换为译文的 PDF。
  - **dual (双语对照版)**：在同一页面中保留原文与译文对照段落的 PDF。
- **交付包 (Delivery Archive / ZIP)**：单次批次中成功生成的 PDF 打包而成的 .zip 压缩文件。
- **交付模式 (Delivery Mode)**：
  - `both`：保留独立 PDF，同时生成 .zip 交付包。
  - `zip_only`：通过临时压缩与条目校验两步提交后，安全清理独立 PDF，仅保留 .zip。
- **作业对象 (Job Object)**：Windows 原生进程管理对象，用于安全追踪并管控子进程树，防止任务取消或窗口关闭时遗留孤儿进程。